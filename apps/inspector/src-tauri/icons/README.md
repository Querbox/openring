# Icons

Replace `icon.png` (1024×1024 PNG) and generate the platform-specific icon set with:

```bash
npm run tauri -- icon path/to/icon.png
```

This produces the `.icns`, `.ico`, and Windows store icons used by the bundler.
