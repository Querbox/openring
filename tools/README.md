# Tools

One-off scripts and helpers that don't deserve their own package: capture file decoders, Python BLE sniffers, hex-dump utilities, etc.

Anything that proves itself reusable should be promoted into a package under `packages/`.
