# Device profiles

Each subfolder describes one smart ring: identifying info, GATT layout, known packets, and reverse engineering notes.

| Folder        | Device     | Status                |
| ------------- | ---------- | --------------------- |
| `optim-ring/` | Optim Ring | Active — Phase 1 work |

New device? Copy `optim-ring/` as a template and fill in what you observe.
